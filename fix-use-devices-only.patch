*** Begin Patch
*** Update File: lib/widgets/device_controls.dart
@@
-  List<dynamic> _deviceListFromStatus() {
-    if (_status == null) return <dynamic>[];
-    if (_status!['devices'] is List) return List.from(_status!['devices']);
-    if (_status!['tanks'] is List) return List.from(_status!['tanks']);
-    return <dynamic>[];
-  }
+  List<dynamic> _deviceListFromStatus() {
+    if (_status == null) return <dynamic>[];
+    final devices = _status!['devices'];
+    if (devices is List) return List<dynamic>.from(devices);
+    return <dynamic>[];
+  }
*** End Patch
